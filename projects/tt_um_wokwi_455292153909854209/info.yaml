# Tiny Tapeout project information (Wokwi project)
project:
  wokwi_id:     455292153909854209       # Set this to the ID of your Wokwi project (the number from the project's URL)
  title:        "simple XOR cipher" # Project title
  author:       "Jan"                    # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "A simple xor cypher. Input the key using reset. encrypt using clock."      # One line description of what your project does
  language:     "Wokwi" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     0       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2


# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "Input bit 1 for key and msg"
  ui[1]: "Input bit 2 for key and msg"
  ui[2]: "Input bit 3 for key and msg"
  ui[3]: "Input bit 4 for key and msg"
  ui[4]: "Input bit 5 for key and msg"
  ui[5]: "Input bit 6 for key and msg"
  ui[6]: "Input bit 7 for key and msg"
  ui[7]: "Input bit 8 for key and msg"

  # Outputs
  uo[0]: "Output bit 1 msg"
  uo[1]: "Output bit 1 msg"
  uo[2]: "Output bit 1 msg"
  uo[3]: "Output bit 1 msg"
  uo[4]: "Output bit 1 msg"
  uo[5]: "Output bit 1 msg"
  uo[6]: "Output bit 1 msg, key head not zeros"
  uo[7]: "Output bit 1 msg, key head not ones"

  # Bidirectional pins
  uio[0]: ""
  uio[1]: ""
  uio[2]: ""
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
